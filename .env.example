# 密钥：从 .env.example 复制后填真实值。.env 被 gitignore，永不提交。
MCPHUB_ADMIN_PASSWORD=
CAUSAL_MEMORY_HTTP_AUTH_TOKEN=
# cron 脚本调用 factor-miner 用的 license key（对应 licenses/licenses.json 里的某把 key）
MCP_LICENSE_KEY=
# quota-platform 管理台令牌（配额管控台 /quota/ 登录用）
QUOTA_ADMIN_TOKEN=
