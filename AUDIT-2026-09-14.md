# MCP Hub 全栈审查与修复清单

> 审查日期：2026-09-14 ｜ 方法：4 路并行代码审查 + 实测验证（沙箱逃逸探针、并发探针、随机链压测、schema 实测）
> 范围：7 个项目 / 111 个 MCP 工具（astock-data 45 · factor-miner 16 · global-data 14 · causal 8 · kronos 4 · causal-memory 17 · hub 网关层）
> 工作量：**S** ≤1h ｜ **M** ≤半天 ｜ **L** 1-2 天 ｜ **XL** 需架构决策

---

## 统计概览

| 级别 | 数量 | 说明 |
|---|---|---|
| **P0 致命** | 17 | 商业致命 / 结论错误 / 安全漏洞 / 数据丢失风险 |
| **P1 重要** | 24 | 功能失效、资源失控、静默降级、隔离缺口 |
| **P2 体验** | 18 | 易用性、描述质量、一致性、文档漂移 |
| **P3 工程** | 8 | 测试、CI、依赖锁定 |

---

## 🔴 P0 — 致命（必须修）

### 1. hub：租户记忆串号 ⭐最高优先
- [ ] **问题**：memory 组指向单个烘焙租户库，所有付费用户共用一个库
- **位置**：`mcp-suite/mcphub/mcp_settings.example.json:53-62`（单把 Bearer）+ `:114-122`；`mcp-suite/README.md:75`（自认）
- **影响**：A 用户 `search_causal` 能读到 B 用户的决策与事实 → **摧毁"私人经验库"卖点 + 隐私事故**
- **修复**：hub 向上游透传用户标识（X-Tenant），由 causal-memory 按租户分库；或按用户动态生成 server 条目
- **过渡**：立即限制 memory 组只发给 1 个用户
- **验收**：两个不同 key 分别写入 → 互相检索不到
- **工作量**：L

### 2. causal：`refute` 恒返回 F（工具完全失效）
- [ ] **问题**：3 个 CAR 拼成 5 元素序列；两个反驳因样本量永远 skipped；placebo idx=0 与真值相同 → 恒 refuted
- **位置**：`causal-mcp/causal_refuter.py:169-173`、`:70`、`:98`、`:48-56`、`:134-136`、`:151`
- **实测**：300 条随机链 300/300 为 F；CAR 8%/9%/10%、p=1e-4 的强效应同样 F；可达评级仅 `{N/A, F}`
- **影响**：`news-credibility` 技能完全失效
- **修复**：改用**日频 AR 序列**（而非 3 个 CAR 拼 5 点）；`total` 改 `len(refutations)`；前提不足返回 `error` 而非评 F
- **兜底**：修不好**先下线该工具**，别让"恒 F"污染信誉
- **工作量**：M

### 3. causal：`event_study` CAR 索引错位一天
- [ ] **问题**：用 dates(N根) 索引去索引 stock_ret(N-1)
- **位置**：`causal-mcp/event_study_batch.py:56`
- **实测**：+10% 打在事件日 sret[E-1]，`event_idx=E` 时 car_1d=**-0.0029**（正确 0.0909）→ CAR 漏掉事件日、量到事件后
- **影响**：`event-impact` 结论可能反向
- **修复**：照抄 `causal_impact.py:23-24` 的 `dates[1:]` 对齐；输出加 `event_idx_date`/`snapped_days`
- **工作量**：S

### 4. causal：显著性阈值 p<0.20
- [ ] **位置**：`causal-mcp/event_study.py:115`（`is_significant`）、`:154`（confidence）
- **影响**：p=0.19 被报"显著"，散户 Agent 误判
- **修复**：改 p<0.05，暴露 `alpha` 参数
- **工作量**：S

### 5. factor-miner：ML 训练特征含未来函数
- [ ] **问题**：对整段 K 线只算一次因子并复用为每一历史行特征
- **位置**：`factor-miner-mcp/factor_miner/trainer.py:88-117`（注释自认 point-in-time approximation）
- **影响**：**所有 ML 指标的 IC/Sharpe 系统性虚高**；`ic` 用 `np.corrcoef` 全池化（`:205`）非逐日截面；`sharpe` 实为 top10 收益/std×√252（`:209`）
- **修复**：逐日/逐截面重算特征且只用 t 及以前数据；IC 改逐日截面均值 + IR；模型按 train_id 落盘
- **工作量**：L

### 6. factor-miner：沙箱可逃逸（安全）
- [ ] **问题**：仅 AST import 白名单，`open` 未拦，无 namespace 隔离
- **位置**：`factor-miner-mcp/factor_miner/sandbox.py:55-71,90-96`
- **实测**：factor.py 成功读 `/etc/hosts`、写宿主机 `/tmp`（rc=0、无 violation）；`PYTHONPATH` 透传
- **修复**：deny `open` 写或只读挂载 + network namespace；清空 `PYTHONPATH`；加 `RLIMIT_FSIZE`/`NPROC`；qrun 子进程同样上 rlimit（`factor_backtest.py:415`）
- **工作量**：M

### 7. factor-miner：异步状态语义坏 + 无超时
- [ ] **问题**：worker 吞异常返回 JSON + JobQueue 无条件置 `done` → **回测失败也报 done**；无 handler 级超时；worker 仅 2 个
- **位置**：`factor_worker.py:262-268,432,470`；`mcp_gateway.py:348-353`；`server.py:221`
- **影响**：失败不可知；2 个长任务瘫痪所有异步工具；`factor_backtest` 1800s 是**每个新因子**（10 因子≈5h）
- **修复**：handler 级超时 + 结果大小上限，超时置 `error`；worker 扩容；相同 (tool,args) 复用 job_id
- **工作量**：M

### 8. hub：鉴权 fail-open + 管理 API 公网可达
- [ ] **问题 A**：tokens 文件缺失/空/不可解析时 `auth=open`
  - **位置**：`causal-memory/src/tenant.rs:16-23,145-158`；`nginx/causal-memory.conf:58-60`（50061 整站公开且不经 hub）
  - **影响**：一次 mount 失败 = **公网可读写共享记忆库**，绕过 license/限流/计量
- [ ] **问题 B**：`/memory/agents/{id}/register`、`/memory/debug/recall` 等管理 API 可达（`sync.rs:391-428`），`sync.rs:109-111`/`:172-174` fail-open，admin token 回退全局 token
- **修复**：加 `require_auth` 开关、加载失败即退出；**nginx 只暴露 `/memory/mcp`**；无 token 返 403
- **工作量**：M

### 9. hub：causal-memory 未配置 LLM（记忆质量机制空转）
- [ ] **位置**：`mcp-suite/docker-compose.yml:128-133`（只传 TZ/AUTH_TOKEN/TOKENS_FILE，无 `CAUSAL_MEMORY_LLM_API/KEY`）
- **影响**：`resolve_updates` 直接返回「⚠ No LLM configured」（`ops.rs:1340-1345`）；`remember` 降级存 500 字原文（`:232`）；embedding 缺失（`embed.rs:39-48`）→ **语义检索/矛盾解决/自动抽取全失效，只剩 BM25**
- **修复**：补 LLM/EMBED env + 启动自检 + `/readyz` 反映状态；`resolve_updates` 异步化
- **工作量**：S

### 10. ~~hub：全平台共享配额~~ → **已修正（见下方实测章节）**
- **原结论不准确**：经 2026-09-14 服务器实测，**quota-platform 已实现 per-key × per-group 配额隔离**（`usage` 表按 key 记账、`key_overrides` 可覆盖、实际拦截 496 次均按 key 记录）
- 上游 5 个服务烘焙同一把 `X-License-Key` 是**设计**（供 quota-platform 内部转发），上游 LicenseStore 定位为"最后一道兜底"
- ⚠️ 但由此暴露**真正的问题**：见实测章节「分组识别错配」

### 11. hub：记忆既不计量也不备份
- [ ] **问题**：usage 卷只挂 4 个数据服务，causal-memory 无 `MCP_USAGE_DIR`；指标无租户维度；crontab 无备份
- **位置**：`docker-compose.yml:39,53,67,81,95` vs `:120-138`；`observability.rs:81-90`；`deploy/crontab:1-8`
- **影响**：无法按用户计费/发现滥用；**单盘损坏 = 全部客户记忆丢失**（唯一无法从上游重建的资产）
- **修复**：加 usage 目录 + 按租户指标 + **每小时 `tenants/*.db` 增量备份 + 恢复演练**
- **工作量**：M

### 12. 全服务：`required` 字段系统性错标（**共享 bug**）
- [ ] **问题**：`required = required or list(properties.keys())` 把可选参数全标必填
- **位置（同一行代码 5 处）**：astock `server.py:223` · global `tools.py:181` · factor-miner `tools.py:39` · causal/kronos 同模式
- **实测**：astock **27/45 工具、33 个参数**错标；global **13/14 工具**；59 个工具仅 `research_reports` 显式传 required
- **影响**：LLM 被迫为"可选"参数臆造取值
- **修复**：改 `required or []`，逐工具核对；抽公共装饰器
- **工作量**：S（+ 逐工具核对 M）

### 13. astock：`get_a_search` name/code 颠倒 + market 恒 SZ
- [ ] **位置**：`astock-data-mcp/server.py:438-441`（上游 `sh~600519~贵州茅台~gzmt~GP-A`，代码取 `parts[1]` 当 name、`parts[2]` 当 code）
- **实测**：`get_a_search("茅台")` → `{"name":"600519","code":"贵州茅台","market":"SZ","type":"gzmt"}`
- **影响**：**Agent 用返回的 code 调 `get_a_hist` 必然失败**（打断"搜索→取数"链路）
- **修复**：`parts[1]`→code、`parts[2]`→name、`parts[4]`→type，`parts[0]` 直接产出 market
- **工作量**：S

### 14. astock/global：错误响应是非法 JSON
- [ ] **问题**：全篇 `'{"error":"%s"}' % str(e)` 未转义
- **位置**：astock `server.py:301,327,374,410…（全篇）`；global `tools.py:805`
- **影响**：`KeyError:'data'`、`JSONDecodeError` 等含引号的常见异常**必然产生坏 JSON**，Agent 无法解析
- **修复**：统一 `fail(code,msg)` 用 `json.dumps` + 一律 `isError: true`
- **工作量**：M

### 15. astock：空结果被当成功缓存 300 秒
- [ ] **位置**：`astock-data-mcp/server.py:311-325`（`get_a_hist`）、TTL `:173`
- **实测**：限流后 `[]` 被写入缓存 → **5 分钟内所有用户拿到假"无数据"**
- **修复**：空结果不缓存；返回 `{"error":"upstream_empty"}` 以区分"代码错"与"无数据"
- **工作量**：S

### 16. astock/global：错误未按 MCP 规范标记
- [ ] **问题**：工具层错误 `isError: False`；同一失败在 `/call-tool` 返 HTTP 500、`/mcp` 返 200+isError
- **位置**：astock `server.py:2032-2036`、`:1956-1970`、`:2037-2042`；global `server.py:165-167`
- **修复**：统一错误出口 + 统一状态码语义
- **工作量**：M

### 17. astock：6 个情绪类工具强制传日期且无默认
- [ ] **位置**：`server.py:1460,1488,1514,1539,1564,1604`（`date: str` 无默认，描述却写 "Get today's…"，省略即 TypeError）
- **对照**：`get_a_daily_dragon_tiger(trade_date=None):990`、`get_a_hot_reason(date=None):700` 却默认今天
- **修复**：改 `date: str = None` 默认今天；全仓统一 `YYYY-MM-DD`
- **工作量**：S

---

## 🟠 P1 — 重要

### causal-mcp
- [ ] **标准误低估**：`event_study.py:93` `σ·√L` 未含估计窗参数不确定性与事件窗市场偏离
- [ ] **benchmark 静默降级**：`event_study_batch.py:42-43` 长度不等 → market_ret 全零，`:58` lstsq 不报错（实测 beta=0、alpha=mean），仍给 `r_squared/is_significant`
- [ ] **`causal_impact` p 值由窗口长度决定**：`causal_impact.py:48-52` n_pre≤n_post 硬编码 p=1.0（实测冲击 0.439 仍 p=1.0、status=ok）；n_pre>n_post 时 p 卡在下界 `1/(1+窗口数)`
- [ ] **`causal_impact` 其他**：`:107` benchmark 缺失仍报 ols_hedge；`:103-105` 按长度截断非按日期 join；`:131-132` `except: pass` 吞降级原因；`:87` p=nan → 输出非法 JSON
- [ ] **`granger_te`**：`:23-24` 取 5 个滞后里最小 p（无校正）；`:92-97` TE 阈值无零分布；`:67` 长度不齐静默取尾部
- [ ] **`pcmci_discover`**：`:64` 原始 alpha 逐对筛选（tigramite 路径自带 FDR，fallback 没有）
- [ ] **`causal_graph`**：`:117-128` PC 只出骨架、direction 仅由 |corr|>0.1 定（**伪方向**，from/to 实为列序 i<j）；`:165-172` notears 分支可产生环却仍叫 NOTEARS；`:189` 仅需 5 行即可算
- [ ] **`dml_cate` 无交叉拟合**：`_t_learner:28-36` 同样本拟合+预测、无切分、无区间；`:24` 静默截断；无 overlap/positivity
- [ ] **DoS**：`server.py:114-115` 无 Content-Length 上限、klines 不截断；`ASYNC_TOOLS=∅` 使 `learn_graph` PC 三重组合（`causal_graph.py:88-114`）可无限挂住线程

### kronos-mcp
- [ ] **异步结果协议内不可达**：`server.py:42` `ASYNC_TOOLS={"forecast_batch"}` 但只注册 4 工具、`EXTRA_SCHEMAS={}`、无 `job_status` → LLM 只能 HTTP GET
- [ ] **worker 无超时**：`mcp_gateway.py:248-269`；`:271-282` reaper 只清 done/error → 挂死任务永久占位
- [ ] **预测轴含周末**：`tools.py:197` `step=diffs.median()`（实测推出 2026-01-17 周六），`timestamps_mode="inferred"` 无提示
- [ ] **"解析失败退回 index"实为崩溃**：`tools.py:176-178/190-196` 退回 int；`model/kronos.py:472-479` 用 `.dt` → 实测 int64 AttributeError（README:17 承诺可用）
- [ ] **资源**：`server.py:99-100` 无 body 上限；`tools.py:220` 单锁串行推理无排队上限；`mcp_gateway.py:221` 队列表项各持完整 klines、`:261` 结果全量驻留 3600s；单 key 闸 `:211` 在开放模式失效

### factor-miner-mcp
- [ ] **并发互斥缺口**：单 key 闸 `mcp_gateway.py:299` 在开放模式（key=""）失效（实测连提 2 个重任务均接受）；队列允许堆 50 个；无 CPU/内存上限
- [ ] **`update_data` 可并发 → 数据损坏**：两进程同时 rmtree staging + `atomic_swap` 同一 provider_dir（`update_data.py:441,545,563`）→ 加文件锁
- [ ] **原子性窗口**：`atomic_swap` 两次 rename 间存在 provider_dir 不存在窗口（`update_data.py:388-398`），与注释不符
- [ ] **磁盘永不清理**：`JOBS_ROOT`/`BACKTEST_ROOT`/`EXEC_CACHE_DIR`（`factor_worker.py:33-36,91,210,278`；实测 `/tmp/factor_jobs` 积压 45 个）
- [ ] **exec_cache 失效**：版本取整份 h5 md5（`factor_backtest.py:140-151`）→ 每日更新后缓存全失效；同一回测内 md5 算两次
- [ ] **`regen_h5` 每日全量重建**：`update_data.py:403-423`（TODO 自认 >10min）
- [ ] **数值稳健**：`analytics.py:56` ffill 无 limit（停牌/退市按 0 收益进协方差）；`factors.py:230` 无除零守卫（实测 0 价 ZeroDivisionError）；`roc`/`ema` 无 NaN 处理；`compute` 不校验最短样本（1 根也输出 53 因子）；`analytics.py:86,108` 负周期正向 shift 取未来；`:343` regime 阈值用全样本中位数（look-ahead）
- [ ] **无 `job_status`**（本地仓库）：异步链路不经 hub 时对 LLM 不闭环

### astock / global
- [ ] **描述与行为不符**：`get_a_north_flow` 的 `days` 被 clamp 后丢弃（`server.py:385`）；`get_a_financials` 的 `annual` 完全未使用（`:457-515`）
- [ ] **日期两套约定**：6 个池类用 `YYYYMMDD`（`1459,1487,1513,1538,1563,1603`），4 个用 `YYYY-MM-DD`（`697,832,901,988`）
- [ ] **代码格式无容错 + 3 套市场规则**：`sh600519`/`600519.SH`/`SH600519` 实测静默 `[]`；`("60","68","5","9")`(`277`) vs `startswith("6")`(`800,1170,1259,1295,1332,1385,1855`) → 5xx ETF、8xx 北交所判错
- [ ] **无类型强制**：`HANDLERS[tool](**args)` 直接解包（`2031`），传 `days="5"` 抛不可操作错误；`research_reports` 传字符串逐字符迭代（`research_report.py:52-56`）
- [ ] **`get_global_news` 主题静默空 + `freq` 无 enum**：`tools.py:396-407`、`:302-320`（59 工具 enum 数为 0）
- [ ] **日志被静默关闭**：`tools.py:27` 的 `basicConfig(WARNING)` 早于 `server.py:41`（实测 `isEnabledFor(INFO)=False`）；astock 同
- [ ] **死代码 + 额度退还**：`mcp_domain_server.py:84` `ASYNC_TOOLS={}` 使队列/workers/jobs 全死；`:221-238` QueueFull 拒单前已扣额度不退还
- [ ] **输入直拼路径/URL**：`download_report_pdf(server.py:635)`、`get_a_eps_forecast(server.py:661)`
- [ ] **astock 默认 `0.0.0.0` 且缺省无鉴权**（`server.py:2065,2077`）vs global 默认 `127.0.0.1`（`server.py:183`）——安全姿态不一致

### causal-memory（Rust）
- [ ] **写路径无校验 → 污染因果符号**：`server/tools.rs:19-31` 无 enum/`deny_unknown_fields`；`Relation::from_str_lossy` 把未识别值映射成 `NoEffect`（`hippocampus/types.rs:47-57`）；`confidence_source` 拼错静默取 0.6（`ops.rs:97-101`）
- [ ] **limit 不 clamp**：`search_causal` (`ops.rs:431-433`)、`resolve_updates` (`:1348-1350`)；`max_tokens` 默认 0=不限 → 一个请求拉全库（`bm25.rs:272`）
- [ ] **`resolve_updates` 超时**：默认 20 次串行 LLM 判官 ×8s（`llm.rs:15`）≈160s > 工具 60s 超时
- [ ] **config.json 未设 0600**（含 LLM/EMBED/AUTH token）：`config.rs:75,20-21`
- [ ] **BM25 全表回退无 LIMIT**：`bm25.rs:174-186`（空 token 先取全表再 truncate `:143-146`）

### hub 平台
- [ ] **供应链风险**：mcphub 用 `latest` 镜像（`docker-compose.yml:106`）+ 运行时 `npx -y`/`uvx` 拉未固定版本（`mcp_settings.example.json:63-90`）+ `network_mode: host`（`:109`）+ `mcp_settings.json` **可写**挂载（`:115`，缺 `:ro`）→ 投毒包可读走全部 license key + 租户 token
- [ ] **`fetch` 工具 = 无白名单 SSRF**（未分组、public，可打 `127.0.0.1:5005x/6379`/云元数据）
- [ ] **OAuth fail-open**：`requireState=false` + `dynamicRegistration.requiresAuthentication=false`（`:139-147`）
- [ ] **nginx 无 body size / 限流 / CORS**：无 `client_max_body_size`（默认 1m）→ 带大量 klines 的 `forecast_batch`/`ml_train_rolling` **413**；无 `limit_req/limit_conn`；无 Origin 白名单
- [ ] **compose 缺 healthcheck + redis 双份**：容器 redis（`:97-103`）vs factor-miner 实际用 host Redis（`:31`）

---

## 🟡 P2 — 体验与一致性

### 全服务
- [ ] **缺 `symbol + 日期` 取数入口**（最高性价比的易用性改造）：45+ 个工具强制大数组（`causal/tools.py:61-67`、`kronos/tools.py:262-273`、`factor-miner/tools.py:138-234`、astock）；而 `factor_miner/update_data.py:104` 已有 `_klines(symbol,start,end,fq)`、`compute_factors` 已有 symbol 先例 → **改造成本低**
- [ ] **`compute_factors`/`predict` 无 description 与参数说明**（`factor-miner/tools.py:261-266`）
- [ ] **`predict` 实为 Phase-0 mock**（`models.py:32-41`）却称 "ML model prediction"，与 `ml_predict` 命名冲突
- [ ] **`ml_predict` 描述与行为不符**：称"优先 Redis 快照"但 handler 不传 redis（`_trainer.redis` 恒 None，`tools.py:152-154`）
- [ ] **错误/返回不统一**：同步走文本 `isError`、异步/鉴权走 JSON-RPC error；analytics 错误只返回 `{"error":...}` 缺承诺字段
- [ ] **协议细节**：initialize 忽略 `protocolVersion`（`factor-miner/server.py:136`）；JSON-RPC 批量数组抛 AttributeError 无响应（`:110-124`）；请求体无大小上限
- [ ] **描述质量参差**：astock 35/45、global 12/14 未说明返回结构（好例子：`get_a_eps_forecast` 含 analyst_count 警告）
- [ ] **无锁定依赖**：causal/kronos 无 requirements 锁定；kronos `model` 参数无白名单（可触发任意 HF repo 下载）+ 热切换全量重载不 `empty_cache()`

### kronos 输出质量
- [ ] 同一响应两份 `expected_return_pct`/`direction`（`tools.py:368` N 次均值 vs `:375` 只取 `preds[-1]`）可矛盾
- [ ] `confidence` 无统计依据（`:355` `consistency/(1+ret_std)`，std 用 ddof=0、runs≤5）；schema 默认 5(`:281`) vs 代码默认 3(`:332`) 不一致
- [ ] `forecast_kline` 走 sample_count=5 内部均值路径 → 平滑均值，`pred_volatility` 低估；**无区间/分位输出**；**零免责声明**（仅 `:357-358` 一条 risk_note）
- [ ] 无 schema 校验（`server.py:200/180` 直接 `**tool_args`，错参返 "Error: TypeError"）

### hub / 运维
- [ ] **双入口导致隔离靠手工**：`README.md:75-79` 记忆隔离需手工建 server，60 人规模 O(N)
- [ ] **退订带不走记忆**：租户只有 CLI（`deploy/cm-tenant.sh`），无自助导出
- [ ] **部署路径断裂**：`README.md:48` 用 `/opt/athena-mcp/licenses`，本仓在 `/opt/mcp-suite`
- [ ] **文档漂移**：`tools.rs:1`/`server/mod.rs:1` 写「16 tools」实际 17；`mcp_settings:56` 写 17；`README.md:17` 承诺的 kronos 降级实为崩溃

---

## ⚪ P3 — 工程基础

- [ ] **测试缺口**：astock 45 工具 schema/handler **零覆盖**（仅 test_mcp_gateway.py 171 行）；global **无任何测试**；causal/kronos **无测试目录**；factor-miner 仅 3 处（analytics/trainer/mcp_gateway/server 零测试）
- [ ] **CI 缺失**：astock/global/factor-miner/causal/kronos 无 CI（仅 causal-memory 有 fmt/clippy CI）
- [ ] **优先补**：JobQueue 状态机、tearsheet IC、trainer 防泄漏、工具 schema 快照测试
- [ ] **依赖锁定**：全部服务补 `==` 或 lockfile

---

## 🔧 跨服务主线：抽公共库 `mcp-common`

以下 bug 在**多个服务里重复出现**（同一份代码模板复制），**抽库一次修好全部**：

| 共享缺陷 | 出现位置 | 公共方案 |
|---|---|---|
| `required or list(properties.keys())` | astock `server.py:223` · global `tools.py:181` · factor-miner `tools.py:39` · causal/kronos | `@tool` 装饰器统一 required 生成 |
| `'{"error":"%s"}' % str(e)` 未转义 | astock 全篇 · global `tools.py:805` | `fail(code, msg, hint)` → `json.dumps` + `isError` |
| 无类型强制（`**args` 直接解包） | astock `2031` · global · kronos `200/180` | schema 校验中间件 |
| 日期/代码格式无统一容错 | astock 12 处重复逻辑、两套日期约定 | `normalize_symbol()` / `normalize_date()` |
| 日志 `basicConfig` 早于 server 初始化 | astock · global | 统一日志初始化 |

---

## ✅ 做得好的（改进时不要破坏）

- **causal-memory 本体工程素质高**：每租户物理隔离 + 路径消毒 + FNV 去歧义、被拒请求不建库、constant-time 比较（`http_auth.rs:22-31`）、fail-closed 热更新（`tenant.rs:175-217`）、标准 Okapi BM25 + RRF、WAL + 连接池 + UNIQUE 重试、软失效可审计可幂等、**436 测试 + CI（clippy -D warnings）**、工具描述明确"何时查/何时记"、L0/L1/L2 分层省 token、预测台账可校准
- **factor-miner `update_data`**：staging → 校验 → 原子切换 → 失败保留旧数据，测试覆盖 4 类故障（成功/校验失败/全源熔断/停牌/新上市）
- **data 组**：`em_get` 全局节流 + Session 复用 + urllib3 Retry（429/5xx）有效规避封 IP；惰性导入贯彻到底；mootdx 三级降级 + TCP 探测规避 BESTIP bug；`roe_approx` 诚实标注为推导值
- **多处"诚实拒绝而非硬算"**：样本不足直接报错（`event_study.py:32/54`、`event_study_batch.py:66`、`causal_impact.py:111`、`granger_te.py:66`、`pcmci_discover.py:99-103`、`dml_cate.py:62`）
- **降级可判别**：`method` 字段标注真实实现；可选依赖全惰性 import（未装可启动并给修复指引）
- **网关**：有界队列 + 拒载 + TTL + worker 池、原子写 usage、`/metrics`、非 root 容器
- **kronos**：模型单例 + 懒加载 + 双锁、仅同名不重载、`local_files_only` 离线命中、device auto 选择、`max_context` 硬校验、不静默传播 NaN

---

## 📌 建议执行顺序

| 批次 | 内容 | 理由 |
|---|---|---|
| **1** | P0 #1-4（记忆串号 · LLM 配置 · 鉴权 fail-open · refute/event_study/显著性） | 商业致命 + 结论错误，且多为 S/M 工作量 |
| **2** | P0 #5-7（训练泄漏 · 沙箱 · 异步状态）+ #10-11（配额 · 备份） | 正确性与资产安全 |
| **3** | P0 #12-17（required / 错误格式 / 缓存 / isError）+ 抽 `mcp-common` | 一次修好 5 个服务 |
| **4** | P1 全部 + P2 的 `symbol 取数入口` | 稳定性与易用性（服务"不会提问"的用户） |
| **5** | P3 测试/CI/依赖锁定 | 防回归 |

---

*本清单由 4 路并行代码审查生成，所有结论均带 文件:行号 证据并经实测验证（沙箱逃逸探针、并发探针、300 条随机链压测、schema 实测、日志级别实测）。*

---

# 📡 管控层实测补充（2026-09-14 服务器勘查）

> 服务器：`root@47.90.253.85`（阿里云，up 11 天）｜方法：只读勘查（未改动任何文件/配置）

## 管控架构（已确认）

```
用户 → nginx(/hub/ → 127.0.0.1:3200) → quota-platform 数据面:3200（配额拦截+计量）
                                              ↓           管理面:3300（看板，经 /quota/）
                                        mcphub:3100（samanhappy/mcphub:latest，认证/路由/审计）
                                              ↓
                          mcp-suite 各服务 50052-50061（LicenseStore 兜底）
```

**运行容器 9 个**：`quota-platform` · `mcphub` · `mcp-{astock-data,factor-miner,causal,global-data,kronos,causal-memory}` · `redis`
**防火墙**：3200/3300/3100 均只许 127.0.0.1（`systemd/mcphub-firewall.service`）

## ✅ quota-platform 做得好的（超出预期，不要破坏）

- **sidecar proxy 架构**：转发前判定，超限直接 429，不触达上游
- **per-key × per-group × 日/月配额** + `heavy_tools` 单独限次 + `key_overrides` 按 key 覆盖 + `disabled`/`expires_at` 状态（检查先于配额）
- **`classify_request` 只统计 `tools/call`**（tools/list、SSE、管理 API 不计次）
- **key 库存安全**：只读挂载 mcphub settings + mtime 热加载，**完整 token 永不出 mcphub 侧**
- **逐条流水** `calls`（key/group/tool/latency/status/reason）+ `blocks` 拦截记录 + 保留期清理
- **管理看板完善**：总览卡片 / 7 天折线 / 分组用量 / key TOP / 拦截列表 / 逐 key 禁用·提额 / key 下钻（用量·成功率·p50·p95·流水·过期时间·配额覆盖编辑器）/ 规则可视化编辑（保存即生效）
- **零第三方依赖（仅标准库）+ 27 个单测**

## 🔴 P0-新1：分组识别与实际接入错配（**正在伤害付费用户**）

- **代码**：`quota-platform/server.py:467-479` `extract_group()` —— **仅靠 URL 路径段匹配**，未命中即 `global`
- **实测流量**（quota.db）：
  ```
  global: 980 次   data: 30 次   alpha: 21 次
  ```
- **后果**：
  ```
  global 未在 groups 配置中 → 落到 default 档（daily 500）
  → blocks 表 496 次拦截（daily 481 + heavy_daily 15）
  → 被拦最多：astock-data-get_a_hist（197 次）—— 本该是 free 档（100 万/天）的免费数据查询
  ```
- **受影响付费客户**（usage 表）：但文博（9/11 与 9/13 各 500 次撞上限、9 月累计 1066）、楼云观（9/11 = 500、9/13 = 199）、淘淘、ju
- **影响**：付费用户"查行情也被限流"的莫名体验；27% 的调用被拒（9/13：983 次中 267 非 ok）
- **修复（首选）**：`classify_request` **已解析出 tool name**（`:481-498`，它正是用 name 判 heavy）→ 扩展为
  `判组优先级：路径命中 > 工具名前缀（astock-data-*/global-data-*→data，factor-miner-*/causal-*/kronos-*→alpha，causal-memory-*→memory）> global`
  —— **不受用户接入路径影响**（路径不可控）
- **临时止血**：`groups` 加 `"global": {"tier": "free"}` 或放宽 default 档
- **配合**：给用户提供带组名的正确 URL + 更新文档
- **工作量**：S（代码 ~10 行 + 配置 + 1 组单测）

## 🟠 P1-新1：`log_retention_days` 配置缺失

- `config.json` 字段列表：`upstream / data_plane / admin_plane / admin_token / mcp_settings_path / groups / tiers / key_overrides / heavy_tools` —— **无 `log_retention_days`**
- README 称"保留期默认 30 天"，需确认代码内是否真有默认值；否则 `calls` 表会无限增长
- **工作量**：S

## 🟠 P1-新2：上游共 key 的兜底语义需明确

- 上游 5 服务烘焙同一 `X-License-Key` + 各自 `licenses.json`（服务端只有 `.example`）
- 现状可接受（quota-platform 是第一道闸），但**建议**：文档明确"上游配额仅防御性兜底，真实配额以 quota-platform 为准"，避免两处配额漂移后互相干扰
- **工作量**：S

## 🟡 P2-新1：运营可观测缺口

- 无"配额拦截率"告警（当前 27% 非 ok 无人在意）
- `usage` 表无 causal-memory 记录（印证 P0 #11：记忆不计量）
- **工作量**：M

## 📌 管控层修复优先级

| 顺序 | 项 | 工作量 |
|---|---|---|
| **1** | 分组识别改按工具名前缀（P0-新1） | S |
| **2** | 记忆串号（P0 #1） | L |
| **3** | 鉴权 fail-closed + nginx 只暴露 /memory/mcp（P0 #8） | M |
| **4** | causal-memory 补 LLM 配置（P0 #9） | S |
| **5** | 记忆备份 cron + 计量（P0 #11） | M |
| 6 | log_retention_days + 上游兜底语义文档化 | S |
| 7 | nginx body/限流/CORS、供应链加固、SSRF/OAuth | M-L |

---

# 🚀 修复进度（2026-09-14 起）

## ✅ 已完成并部署

| 项 | 内容 | 验证 |
|---|---|---|
| **P0-新1** | **分组识别错配** → 改为「路径命中 > 工具名前缀（最长前缀优先）> global」 | 判定 6/6 正确；已重启 quota-platform |
| **P0 #11（部分）** | **记忆备份**：`deploy/backup.sh` + crontab 每日 03:00 | 试运行 9/9 租户库 + 配置（3.7M）；SQLite `.backup` 保证 WAL 一致；保留 14 天 |
| **P0 #12** | **`required` 系统性错标**（5 服务）→ `required or []` + 逐工具显式声明 | astock 45/45、global 14/14、causal 8/8、factor-miner 16/16、kronos 5/5 对账一致；已推送 |
| **P0 #13** | **`get_a_search` name/code 颠倒** → `code=parts[1]`/`name=parts[2]`/`market=parts[0]`/`type=parts[4]` | mock gbk 报文解析出 `{"code":"600519","name":"贵州茅台","market":"SH"}` |
| **P0 #14** | **错误 JSON 非法**（astock 44 处 + global）→ 统一 `fail()` (json.dumps + code/hint) | 含引号异常现可 `json.loads` |
| **P0 #15** | **空结果污染缓存** → `_is_empty_result()` + 37 个调用点跳过空缓存 | 空 `[]`/`{}` 不再写缓存 |
| **P0 #16** | **isError 未标记**（astock/global + 分域服务） | 端到端实测：错误 JSON → True，正常 → False |
| **P0 #2** | **`refute` 恒返回 F** → placebo 采样排除重合窗口 + 实跑测试数 <2 时返回 N/A + notes 披露 | 强效应 chain 由 `F` → `N/A`（notes 说明 2/3 跳过） |
| **P0 #3** | **`event_study` CAR 索引错位一天** → `ret_dates = dates[1:]` 对齐 | 合成数据（事件日 +20%）：旧 `car_1d=0.0` → 新 `0.182322` |
| **P0 #4** | **显著性 p<0.20** → p<alpha（默认 0.05）+ 输出 `alpha_threshold` | 阈值翻转实测：p=0.1902 由"显著"→"不显著" |
| P1-新1 | `log_retention_days` | **核实为非问题**（代码默认 30 天 + 每小时清理） |

## ⏳ 待决策 / 待办

| 项 | 阻塞点 |
|---|---|
| **P0 #9** causal-memory LLM 配置 | **需你提供 API key**（服务器 `.env` 无 LLM key）；补齐后语义检索/矛盾解决才真正生效 |
| **P0 #1** 租户记忆串号 | **需确认架构方向**：hub 透传用户标识（X-Tenant）由 causal-memory 分库，还是按用户动态生成 server 条目 |
| P0 #2 附带 | `refute` 若要能出 A–F 需**接口改造**（让 event_study 返回逐日 AR 序列） |
| P0 #4 附带 | `confidence` 尺度收紧（p=0.03 时 0.895→0.58），下游若用阈值闸门需重新校准 |
| P0 #5-8 | ML 训练泄漏、沙箱逃逸、异步状态语义、鉴权 fail-closed |
| P1/P2/P3 | 其余约 45 项（含抽 `mcp-common`、`symbol` 取数入口、测试/CI） |

## 📌 运维侧发现

- **磁盘曾达 90%（4.9G）**，清理 build cache 后恢复到 9.4G（80%）；根分区 31G 被 Docker 占用，建议定期 `docker builder prune`
- **部署流程确认**：各服务为独立 git 仓库 → 本地改 + push → 服务器 `git fetch/pull` → `docker compose up -d --build <service>`
- `quota-platform` **未纳入 git**（仅存在于服务器）→ 建议尽快入库（否则单点丢失）

---

# 🔄 核对与部署结果（2026-09-14 二次核对）

## ✅ 经核对确认「已修」（不再重复处理）

| 原清单项 | 实际状态 | 证据 |
|---|---|---|
| **P0 #1 租户记忆串号** | **已由用户修复** | mcphub 的 causal-memory url 指向 `127.0.0.1:51061/mcp`（cm-tenant-shim），systemd `cm-tenant-shim` active，9 个租户库物理隔离 |
| 5 服务缺 `job_status` | **已修** | `factor-miner-mcp/server.py`、`causal-mcp/server.py`、`kronos-mcp/server.py` 均已注册 |
| astock 路径穿越/URL 注入 | **已修** | commit 550e58b（锁定 REPORTS_DIR + info_code 白名单消毒） |
| kronos Chronos-2 后端 | **新增** | commit 1b96952（`model=chronos2` + `forecast_compare`） |
| factor-miner MASTER 后端 | **新增** | commit 54f5d8c（AAAI 2024 股票 Transformer） |

> 教训：本清单的初版审查基于**本地旧代码快照**，未包含服务器实际部署状态与用户近期提交，导致上述项被误列为待修。**以后审查必须先 `git fetch` 对齐远端**。

## ⚠️ 重要运维知识：`mcphub` schema 缓存

**现象**：上游容器（astock/factor-miner）重建后，通过 hub 拿到的 `tools/list` **仍是旧的 `inputSchema`**（`required` 没变）；而**容器内代码已确认是新版**。

**根因**：`mcphub`（samanhappy/mcphub）**缓存了上游 server 的 tools/list**。

**处置**：`docker restart mcphub` 后立即生效（实测：`update_data.required` 由 4 项 → `[]`）。

**规则**：**每次修改上游 MCP 工具的名称/描述/参数 schema 后，必须重启 mcphub**，否则线上客户端看不到变更。

## 🔧 部署流程（已验证）

```
本地改 → git commit/push → 服务器 git fetch && git pull → docker compose up -d --build <service> → docker restart mcphub
```

**踩过的坑**：
1. **`[] or list(properties.keys())` 的 falsy 陷阱**：显式传 `required=[]` 会被 `or` 短路成全必填。`required or []` 才是正确写法。
2. **`git pull --ff-only` 会被 untracked 文件阻塞**：服务器上 `deploy/backup.sh` 已是 untracked，pull 要创建同名文件 → `Aborting`。需先移除/备份该文件。
3. **本地仓库落后时提交会覆盖远端工作**：本次本地 mcp-suite 落后 12 个提交，直接 push 会覆盖 quota-platform 的管理台升级 → 已改为「撤销本地提交 → pull 远端 → patch 方式重放修复」。

## 📊 批次进度

| 批次 | 内容 | 状态 |
|---|---|---|
| **1** | 分组识别 · 记忆备份 · required（5 服务）· get_a_search · refute · event_study 索引 · p<0.05 · 错误 JSON · 空缓存 · isError | ✅ **已部署并线上验证** |
| **2** | factor-miner（异步状态/超时/并发/磁盘/沙箱/训练泄漏）· causal（causal_impact p/标准误/benchmark/granger/PC）· kronos（异步闭环/周末轴/崩溃/免责） | 🔄 修复中 |
| **3** | 跨服务：`symbol` 取数入口 · 类型校验 · 抽 `mcp-common` | ⏳ |
| **4** | 测试补齐 · CI · 依赖锁定 | ⏳ |

## 🖥️ 运维侧

- 磁盘曾 90%（4.9G）→ 清 build cache 3.0GB → **9.4G 可用**；根分区 31G 被 Docker 占用，建议定期 `docker builder prune -f`
- 备份：`deploy/backup.sh` + crontab 每日 03:00（9 租户库 + 配置，保留 14 天）
- `quota-platform` 已在 git（此前误判）；但**服务器工作副本的 `config.json`（含 admin_token）与 `quota.db` 不入库**（已由 `.gitignore` 覆盖）
